/**
 * Sampler — placeholder for future elementary-audio DSP integration.
 *
 * In a later task this module will use @elemaudio/core and
 * @elemaudio/web-renderer to build a declarative DSP graph for sample
 * playback with effects (reverb, delay, filtering, etc.).
 *
 * For now the AudioEngine handles playback directly via Web Audio API.
 */

// This file is intentionally minimal for the prototype.
// It exists to establish the module structure for Task 2.2+.

export {};
